#include<conio.h>
#include<stdio.h>
void sum(int n);
void main()
{ int a;
printf("enter a number");
scanf("%d",&a);
sum(n);
getch();
}
void sum(int n)
{int sum=0;
while(n!=0)
{int rev=n%10;
sum=sum+rev;
n=n%10;
}
printf("sum of digits=%d",sum);
}
